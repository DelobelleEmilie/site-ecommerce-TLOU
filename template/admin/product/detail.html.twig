{% extends "shared/layout.html.twig" %}

{% block title %}
    Administration des produits
{% endblock %}

{% block content %}

    <div class="container">
        <!-- #Se charge ici de vérifier que la valeur de la variable passée dans le contrôleur n’est pas «null» ni égale à «false».
        #Dans ce cas les informations du produit peuvent être affichées
        #Dans le cas où la variable «entity» est «null» ou égale à «false», un message indique que le produit n’existe pas. -->
        <div class="m-3">
            {% if entity %}
                <div class="col-12">
                    <h3 class="mb-5">{{ entity.label }}</h3>
                </div>

                <div class="row">
                    <div class="col-md-4 d-flex justify-content-center">
                        {% if entity.image %}
                            <img src="{{ entity.image }}" class="rounded-start object-fit-contain" alt="...">
                        {% else %}
                            <img src="https://fakeimg.pl/350x200/?text=Image" class="img-fluid rounded-start" alt="...">
                        {% endif %}
                    </div>
                    <div class="col-md-5 text-secondary">
                        <p>{{ entity.description }}</p>
                    </div>
                    <div class="col-md-3 bg-light text-center p-5">
                        <h5 class="h1 text-primary">{{ entity.price }} €</h5>
                    </div>
                </div>
            {% else %}
                <p>Aucun produit n'est défini !</p>
            {% endif %}
        </div>
        <a
                href="{{ listUrl }}"
                class="btn btn-secondary"
        >
            <i class="fa-sharp fa-solid fa-arrow-left-long"></i>
             
            Retourner à la liste
        </a>
        <a
                href="{{ editUrl }}"
                class="btn btn-primary"
        >
            <i class="fa-sharp fa-solid fa-pen"></i>
             
            Modifier le produit
        </a>
    </div>

{% endblock %}